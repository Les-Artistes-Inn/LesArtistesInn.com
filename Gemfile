source 'https://rubygems.org'
gem 'jekyll'
gem 'jekyll-redirect-from'
gem 'jekyll-seo-tag'
gem 'jekyll-sitemap'
gem 'jekyll-minimagick'
# install with sudo gem install bundler; bundle install
# remove all gems with sudo gem uninstall -aIx
